# Release: deployment

Default deployment Not needed. On explicit request, verify existing repository-owned workflow, exact environment, authorization, migrations, rollback, monitoring. Show exact invocation and obtain fresh immediate consent. Missing/ambiguous evidence stops; never guess command, environment, or alternate route. Verify result before any closure claim.
