- For this project, a release means creating a new branch (if none is created), commit and create PR

- when the config is modified, make sure the changes (as in the default values) are reflected in .harnessctl/config.yml and in the python config generator from the installer

- stop using sub-agents -> the ones available are configured for particular flows and are very slow

- when working on the docs website is is futile to perform visual tests - the website's theme was not created yet. My interest for now is content only.
