# Continue: checkpoint reconciliation

Compare all matching active checkpoints with authority. Show contradictions and proposed logical current record. User resolves ambiguity. Never choose by timestamp, merge records, or silently discard.

After confirmed/verified replacement, supersede selected stale record and tombstone confirmed stale duplicates using configured memory capability. Keep partial failure visible; next attempt revalidates full authority.
