# Work Release

Activate `sdlc`; read `references/release.md`. Apply it to current input.

Resolve one verified Epic and perform only confirmed release actions. Load
`references/release-deploy.md` only after explicit deployment request.

Default stop is ready pull request; merge needs fresh explicit consent. Before checkpoint
work, read `references/checkpoint.md`. Return compact SDLC result.
